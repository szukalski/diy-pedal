{
    "$schema": "../schemas/pedal-bom.schema.json",
    "build": [
        {
            "buildName": "test",
            "buildComponents": [
                {"id": "test"}
            ]
        }
    ]
}